import { StyleSheet } from "react-native";
import colors from "../../../resource/Colors";

export const AccountPageStyle = StyleSheet.create({
    contain: {
      flex:1,
      backgroundColor:colors.background,
    },
});
